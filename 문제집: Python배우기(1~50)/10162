T = int(input())    # 요리시간
if T%10!=0:     # 3개의 버튼으로 초를 맞출 수 없으면 -1 출력
    print(-1)
else:
    a = T//300  # a버튼 
    T%=300
    b = T//60   # b버튼 
    T%=60 
    c = T//10   # c버튼 
    print(a,b,c)
