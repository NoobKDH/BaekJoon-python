H, M = map(int, input().split()) //H = Hour;시간, M = minute;분
use_time = int(input()) //use_time = 사용 시간

reH = H // reH = 출력 시간
reM = M + use_time //reM = 출력 분


while reM >= 60:
    reH += 1
    reM -= 60
    if reH == 24:
        reH = 0

print(reH, reM)
