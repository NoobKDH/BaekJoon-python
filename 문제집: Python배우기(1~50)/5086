while(1):   
    a,b = map(int,input().split())  # 첫 번째, 두 번째 숫자 입력
    if a==b==0: # a,b가 0이라면 종료
        break
    else:
        if a>b:     # a가 b보다 크면 배수 확인
            if a%b==0:
                print("multiple")
            else:
                print("neither")
        elif a<b:   # b가 a보다 크면 약수 확인
            if b%a==0:
                print("factor")
            else:
                print("neither")
