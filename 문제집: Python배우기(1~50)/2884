h, m = map(int, input().split()) # 시간, 분 입력
if h==0:    # "시"가 0으로 주어진 경우
    if m-45<0:  # 45분을 뺐을 때 0 이하이면
        h = 23  # "시"를 23으로
        m = m-45+60
    else:
        m=m-45
else:
    if m-45<0: 
        h -=1 # "시"에서 1시간 차
        m = m-45+60
    else:
        m = m-45
print(h,m)
